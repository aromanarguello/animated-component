import * as React from "react";
import { Card } from "../";

const Notification = () => {
  return (
    <Card width={150} height={50}>
      <p>Success!</p>
    </Card>
  );
};

export default Notification;
